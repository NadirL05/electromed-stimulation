# Railway Services Configuration

Ce fichier documente les services Railway pour ElectroMed.

## 🎯 Services à Créer

### 1. Service Frontend (racine du repo)

**Configuration :**
- **Nom** : `electromed-frontend`
- **Root Directory** : `/` (racine)
- **Build Command** : `npm ci && npm run build`
- **Start Command** : `npm run start`
- **Port** : Automatique (Railway utilise $PORT)

**Variables d'environnement requises :**
```
VITE_SUPABASE_URL
VITE_SUPABASE_ANON_KEY
VITE_API_URL
VITE_STRIPE_PUBLIC_KEY
```

### 2. Service Backend (dossier /backend)

**Configuration :**
- **Nom** : `electromed-backend`
- **Root Directory** : `/backend`
- **Build Command** : `npm ci`
- **Start Command** : `npm run dev`
- **Port** : 3000

**Variables d'environnement requises :**
```
STRIPE_SECRET_KEY
STRIPE_WEBHOOK_SECRET
SUPABASE_URL
SUPABASE_SERVICE_KEY
PORT=3000
FRONTEND_URL
```

## 📝 Notes Importantes

1. Déployez le **backend d'abord** pour obtenir son URL
2. Utilisez l'URL du backend dans `VITE_API_URL` du frontend
3. Utilisez l'URL du frontend dans `FRONTEND_URL` du backend
4. Les fichiers `railway.json` sont présents dans chaque service

## 🔗 Connexion des Services

Frontend → Backend : Via `VITE_API_URL`
Backend → Frontend : Via `FRONTEND_URL` (pour CORS)

Consultez `docs/RAILWAY_DEPLOYMENT.md` pour le guide complet !
